function blog() {
    
    function init() {
        var deltaY = 0;
        document.addEventListener('scroll', function(e) {
            console.log(e);            
        });
    }

    /** init blog javascript */
    // init();

    return {
            }
}

var Blog = blog();

